docker container stop openspeedtest
docker system prune --all --force

docker image pull openspeedtest/latest

docker network create openspeedtest

docker run \
  --detach \
  --name openspeedtest \
  --network openspeedtest \
  --publish 8013:3000 \
  --restart unless-stopped \
  openspeedtest/latest
